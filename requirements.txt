# Core — needed by every script.
numpy>=1.24
pandas>=2.0
scipy>=1.11
scikit-learn>=1.3

# Training. Install the torch build that matches your CUDA version first, e.g.
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu128
# The ensemble scripts do not need torch at all.
torch>=2.1
torchvision>=0.16
timm>=1.0.0
tqdm>=4.65
Pillow>=10.0
opencv-python>=4.8      # ink-feature extraction in train_v5b_dinov2.py

# Optional.
lightgbm>=4.0           # LightGBM stacking in ensemble_final_tricks.py and experiments/stacking.py
