# Competition data — not redistributed, download from Kaggle
icdar-2026-circleid-pen-classification/
*.zip

# Model checkpoints — ~15 GB total, far past GitHub's limits
checkpoints_*/
*.pt
*.pth

# Training run outputs — logits and submissions are regenerated by the scripts.
# artifacts/ is deliberately NOT ignored: those logits are committed on purpose.
outputs_*/
submission*.csv

# DINOv2 source + weights, fetched automatically by train_v5b_dinov2.py on first run
dinov2_repo/
dinov2_vitb14_reg4_pretrain.pth

# Python
__pycache__/
*.py[cod]
.venv/
venv/
env/
*.egg-info/
.ipynb_checkpoints/

# Editors / OS
.vscode/
.idea/
.DS_Store
Thumbs.db
